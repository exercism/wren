class Item {
  construct new(weight, value) {
    Fiber.abort("Remove this statement and implement this function")
  }
  weight { Fiber.abort("Remove this statement and implement this function") }
  value { Fiber.abort("Remove this statement and implement this function") }
}

class Knapsack {
  static maximumValue(maximumWeight, items) {
    Fiber.abort("Remove this statement and implement this function")
  }
}
